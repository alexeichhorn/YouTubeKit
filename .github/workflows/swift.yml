name: Swift Unit Tests

on: push

jobs:
  build:
  
    strategy:
      matrix:
        swift: ["5.8", "5.9", "5.10", "6.0"]
    
    runs-on: macos-latest

    steps:
    #- uses: swift-actions/setup-swift@v2
    - uses: NeedleInAJayStack/setup-swift@4e17dcd69a660eea371d715802c219601e76f50a # temporary (for testing)
      with:
        swift-version: ${{ matrix.swift }}
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v

    # temporarily disabled because YouTube blocked GitHub actions url
    #- name: Run tests
    #  run: swift test -v
